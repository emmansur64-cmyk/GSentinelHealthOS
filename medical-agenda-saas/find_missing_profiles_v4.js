const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { try { const allUsers = await prisma.user.findMany({ include: { doctorProfile: true }, select: { id: true, name: true, email: true, role: true, tenant_id: true, doctorProfile: true } }); const filtered = allUsers.filter(u => (u.role === "doctor" || u.role === "medico") && !u.doctorProfile).map(({doctorProfile, ...u}) => u); console.log("MISSING_PROFILE_JSON:" + JSON.stringify(filtered)); } catch (error) { console.error(error); } finally { await prisma.$disconnect(); } } main();
