# BRAIN CORE CONTRACT GAP MATRIX

Fecha: 2026-05-15
Alcance: mapeo contrato objetivo vs codigo actual, sin separacion fisica.

| contrato objetivo | archivo actual relacionado | funcion actual relacionada | existe equivalente (si/no/parcial) | brecha | riesgo | cambio minimo futuro | prioridad |
|---|---|---|---|---|---|---|---|
| ChatBrainRequest (modo doctor_professional/clinical_support) | `medical-agenda-saas/src/lib/brain-client.ts` | `callBrainDecide` | parcial | payload tipado pero sin `allowed_tools/forbidden_tools` obligatorios | mezcla de herramientas fuera de dominio | agregar validador previo de contrato MB-Chat antes de llamar Brain | alta |
| ChatBrainRequest prohibe appointment_write | `medical-agenda-saas/src/chat/chat.service.ts` | `handleDoctorChat` | parcial | se fuerza `assistant_mode=doctor_professional`, pero no hay policy de tools | posible deriva hacia agenda write en futuras integraciones | enforcement tool-policy en capa contrato | alta |
| SecretaryBrainRequest | `medical-agenda-saas/src/app/api/import/agenda/parse/route.ts` | `POST` | parcial | existe flujo de import, pero no existe contrato formal `secretary_ingestion` | ingestion administrativa sin gate unificado de dominio | encapsular request en contrato y validar `import_mode` + tools | alta |
| Secretary prohibe clinical diagnosis | `medical-agenda-saas/src/app/api/import/agenda/parse/route.ts` | `buildAgendaImportGuidance` uso indirecto | no | no hay bloqueo explicito de herramientas clinicas en contrato | contaminacion de dominio administrativo con clinico | denegar tools clinicos en contrato MB-Secretaria | alta |
| WhatsappBrainRequest | `medical-agenda-saas/src/lib/whatsapp/metabrain-assistant.ts` | `generateWhatsAppMetaBrainReply` | parcial | hay contexto WA, pero no contrato formal `appointment_booking` con tools | WA podria consumir rutas clinicas profundas | crear contrato WA y enforcement fail-closed | alta |
| Whatsapp prohibe clinical diagnosis | `medical-agenda-saas/src/lib/whatsapp/conversation-engine.ts` | `handleMessage` | no | el flujo WA maneja turnos + fallback IA sin contrato de bloqueo clinico | riesgo regulatorio por diagnostico en canal admin | agregar guard de modo antes de llamadas IA | alta |
| BrainCoreResponse con allowlist de acciones | `brain/app.py` | `orchestrate` response actual | no | metadata existe, pero no `proposed_actions` formal con allowlist valida | ejecucion de acciones sin contrato central | introducir `BrainCoreResponse` tipado y validador de acciones | alta |
| BrainAction allowlist centralizada | `brain/services/orchestrator.py` | `_handle_booking`, `_handle_cancellation`, etc. | no | acciones estan implcitas en funciones, no como enum de contrato | inconsistencia entre canales y auditoria incompleta | crear enum/allowlist central de acciones y mapping por modo | alta |
| Guard doctor_professional no triage patient-facing | `brain/contracts/routing.py`, `brain/core/decision_core.py` | `get_capabilities`, `process_input` | si | guard ya presente para triage automatico | bajo | mantener y cubrir por test de regresion | media |
| Guard assistant_mode desconocido fail closed | `brain/contracts/routing.py` | `AssistantMode.from_raw`, `build_contract` | parcial | cae a `generic_non_clinical`, pero no hay error contrato de IO explicito | comportamiento ambiguo entre fallback y rechazo formal | sumar validador de IO que rechace modo desconocido cuando aplique contrato MB | media |
| Tenant handling consistente | `brain/integration/api_client.py`, `brain/core/state_manager.py` | `_headers`, `_scope_phone` | si | maneja `client_id`/`clinic_id` y state por clinic+phone | bajo | conservar como base de separacion logica por dominio | media |
| Entrada doctor chat -> Brain unificada | `medical-agenda-saas/src/chat/chat.service.ts` | `handleDoctorChat` | parcial | usa Groq + Brain + fallback local (3 caminos) | drift funcional y auditabilidad compleja | fijar orden de providers por dominio MB-Chat y normalizar respuesta | media |
| Entrada WA -> Brain unificada | `medical-agenda-saas/src/lib/whatsapp/metabrain-assistant.ts` | `generateWhatsAppMetaBrainReply` | parcial | combina `callBrainDecide`, `callGroqDoctorChat`, `metabrain.decide` | mezcla clinico/admin en WA | forzar `assistant_mode=appointment_booking` y deny clinical tools | alta |
| Contrato de import (preview/apply) | `medical-agenda-saas/src/app/api/import/agenda/parse/route.ts` | `POST` | parcial | `import_mode` existe en objetivo pero no como contrato de Brain | apply sin capa de accion estandarizada | mapear salida a `document.parse_preview`/`document.import_schedule` | media |
| Legacy `/api/v1/brain/decide` vs `/orchestrate` | `api/app/api/v1/endpoints/brain_decide.py`, `brain/app.py` | `brain_decide`, `orchestrate` | no | contratos distintos activos en paralelo | divergencia funcional y de seguridad por canal | deprecacion controlada del legacy con adapter contract | alta |
| Config provider por dominio | `medical-agenda-saas/src/lib/groq-doctor-chat.ts`, `.../whatsapp/groq-assistant.ts`, `.../document-ai.ts` | `getConfig`/`resolveApiKey` | parcial | hay variables separadas parciales, no set canonico MB por dominio | fallback cruzado de claves entre dominios | establecer env canonica por dominio + lectura estricta por modo | alta |
